# Where the frontend looks for the backend API (see server/).
# Leave as-is for local dev (npm run dev in both app/ and server/).
# For a real device/simulator build (Capacitor), this can't be localhost —
# see CAPACITOR.md, section 3.
VITE_API_BASE_URL=http://localhost:8787
