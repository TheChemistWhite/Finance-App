<?xml version="1.0" encoding="utf-8"?>
<!--
  DEV ONLY: allows the app to talk to the local backend over plain HTTP.
  10.0.2.2  = the host machine's loopback as seen from the Android emulator.
  localhost = for good measure when debugging on-device via adb reverse.
  Remove this (or restrict it) before shipping a production build that
  points at a real HTTPS backend.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
